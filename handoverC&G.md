# handoverC&G.md - Gemini CLI & Claude Code 共通進捗管理

## ⚠️ 重要：AIへの指示
1. ユーザーは **Gemini CLI** と **Claude Code** を併用して作業しています。
2. 両方のAIは、必ずこの `handoverC&G.md` の内容を確認し、進捗を同期・管理してください。
3. 必要に応じて、他の関連ドキュメント（`GEMINI.md` やプロジェクト内の他の .md ファイル）にも進捗や決定事項を反映させてください。

---

## 📅 本日の進捗 (2026-04-03)

### ✅ 完了事項
- **動画生成の同期問題を根本解決**:
    - **実測尺ベースの構築**: これまで「文字数からの推定尺」でHTMLを組んだ後に「実測音声」を生成していたため、ズレが発生していた。これを「音声先行生成 → 実測秒数をHTMLの登場ディレイに反映」というフローに逆転させ、字幕と音声の不一致（被り）を物理的に解消。
- **VoiceVox辞書のネイティブ化**:
    - **姓名分割登録**: `sanitizeForVoiceVox` 内の日本代表選手・監督名を「苗字[全角スペース]名前」形式（例：森保　一）にアップデート。VoiceVoxが自然なイントネーションと「間」で読むように改善。
    - **主要監督の追加**: ポステコグルー、グアルディオラ、アルテタなどの読みも補正。
- **画像取得パイプラインの知能化**:
    - **監督連想ロジック**: DeepSeek V3/Claude に対し「チーム名があれば必ず監督名も抽出せよ」とプロンプトを強化。ニュースが選手個人でも、監督の画像を確実に確保。
    - **ハッシュタグ連動**: 生成されたハッシュタグ（#森保一 など）からも固有名詞を抽出し、Wikipedia検索（wikiWords）に自動反映。

### 🚀 現在の状況
- 全ての改修が VPS にデプロイ完了。
- **負荷テスト中**: VPS上で最新3件の動画同時生成（VoiceVox実測同期モード）を実行し、生成速度と安定性を検証中。

### 📝 次のタスク（ToDo）
1. **生成時間の分析**: 音声実測ステップが追加されたことによる生成時間の変化をチェック。
2. **「監督画像」のヒット率検証**: 森保監督や海外監督の画像が意図通り配置されているか、生成済み動画を目視確認。
3. **自動投稿ロジックの最終調整**: 同期が完璧になったため、いよいよ YouTube への自動アップロードを本格検討。

---

## 📅 本日の進捗 (2026-04-08)

### ✅ 完了事項
- **環境のリセットと再強化 (Full Rebuild)**:
    - **Revert & Clean**: 一時的に不安定になった環境を今朝の安定版 (`45ac677`) に戻した上で、必要な機能を再実装。
    - **不変IDの実装**: 各案件に `MMDDNNN` 形式の固有IDを付与。定刻のJSON更新でリストがズレても、動画ファイルやサムネイルとの紐付けが壊れないようにした。
    - **ネタ取得の増量 & バランス調整**:
        - 1回あたりの取得を **20件** (Reddit:12 / Japan枠:3 / RSS:5) に大幅増量。
        - **クソスレガード**搭載: スコアやコメント数が極端に少ない「中身のないネタ」を自動でフィルタリングするロジックを導入。
    - **オンデマンド画像取得**:
        - 定刻実行では「テキスト生成」まで。画像取得はランチャーで案件を選んで「自動生成」を押した際に、**選んだIDだけをピンポイントで取得**する仕組みに変更。VPSの容量とAPIの節約を実現。
    - **UIの大幅改修**:
        - **アコーディオン表示**: 案件一覧を「取得時間ごと」にグループ化して折りたたみ表示。大量の案件（1日100件超）でも視認性を確保。
        - **UI整理**: 403エラーの原因だった「今すぐ抽出」ボタンを完全に削除し、安定した定期取得ベースに一本化。
- **VPS同期**: 手動での `git reset --hard` および `pm2 restart` により、VPS側のランチャーも最新状態に更新済み。

### 🚀 現在の状況
- システムは非常に安定しており、大量のネタを効率よく裁ける体制が整った。
- 不変IDの導入により、YouTube投稿ランチャー側での動画とメタデータの不一致問題も解決。

### 📝 次のタスク（ToDo）
1. **運用テスト**: 20件×7回の自動収集が明日まで問題なく回るか監視。
2. **投稿効率化**: YouTube投稿画面でも、IDベースの管理がうまく機能しているか最終確認。

---

## 💡 技術・運用メモ
- **固有ID**: `id: "0409001"` のような形式。動画ファイル名も `日付_ID.mp4` に統一。
- **クソスレガード基準**:
    - Japan枠: Score 50+, Comments 20+
    - Reddit通常: Score 15+, Comments 10+
- **VPS再起動**: `ssh root@37.60.224.54 "pm2 restart soccer-yt"` で最新コードが反映される。
- DeepSeek V3 は `AI_PROVIDER=deepseek` で安定稼働中。1記事あたりの選定コストは1円以下。
- VoiceVox 接続エラーは VPS (Docker) 環境であれば解決済み（localhost:50021）。
- 履歴管理 (`seen_history.json`) は 30日間保持を継続。
