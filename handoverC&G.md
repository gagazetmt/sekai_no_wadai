# handoverC&G.md - Gemini CLI & Claude Code 共通進捗管理

## ⚠️ 重要：AIへの指示
1. ユーザーは **Gemini CLI** と **Claude Code** を併用して作業しています。
2. 両方のAIは、必ずこの `handoverC&G.md` の内容を確認し、進捗を同期・管理してください。
3. 必要に応じて、他の関連ドキュメント（`GEMINI.md` やプロジェクト内の他の .md ファイル）にも進捗や決定事項を反映させてください。

---

## 📅 本日の進捗 (2026-04-01)

### ✅ 完了事項
- **Gemini CLI セットアップ**: 
    - ペルソナ「ミア」のグローバルメモリへの定着完了。
    - `GEMINI.md` へのプロジェクト基本ルールとインフラ情報の集約。
- **引き継ぎドキュメントの刷新**:
    - `GEMINI_HANDOVER.md` を削除し、本ファイル `handoverC&G.md` による共同管理体制へ移行。
- **自動素材収集パイプラインの稼働確認 (2026-04-01)**:
    - ローカルPCでの定刻ネタ収集・AI生成・VPS転送が成功。
    - VPS側での自動画像取得トリガーおよび画像セットアップが正常に動作することを確認。
- **ランチャーUI改修 (2026-04-01)**:
    - 案件を個別に選択して生成できるよう、サイドバーにチェックボックスと「全選択/解除」機能を追加。
    - 動画生成スクリプトを拡張し、特定のインデックスのみを生成可能にした。

### 🚀 現在の状況
- 全自動素材収集システムが安定稼働中。
- ランチャーUI上で個別案件の選択生成が可能になり、デバッグ効率が大幅に向上。
- 現在、38件のネタの中から選別して動画生成テストを行える状態。

### 📝 次のタスク（ToDo）
1. **動画生成テスト（個別選択）**: 新しく追加したチェックボックスで数件選び、正常に生成されるか確認。
2. **生成動画の品質チェック**: プレビューと音声・スライドのタイミングにズレがないか、slide_5でクラッシュしないか検証。
3. **自動生成への拡張**: 運用が安定したら、収集から生成までを完全自動化する検討を開始。

---

## 💡 技術・運用メモ
- DeepSeek V3 をメインAIとして使用中 (`AI_PROVIDER=deepseek`)。
- 動画生成は `POOL_SIZE=8` で並列処理。
- VPSデプロイは `git push` による自動デプロイを利用。
