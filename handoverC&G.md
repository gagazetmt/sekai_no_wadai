# handoverC&G.md - Gemini CLI & Claude Code 共通進捗管理

## ⚠️ 重要：AIへの指示
1. ユーザーは **Gemini CLI** と **Claude Code** を併用して作業しています。
2. 両方のAIは、必ずこの `handoverC&G.md` の内容を確認し、進捗を同期・管理してください。
3. 必要に応じて、他の関連ドキュメント（`GEMINI.md` やプロジェクト内の他の .md ファイル）にも進捗や決定事項を反映させてください。

---

## 📅 本日の進捗 (2026-04-03)

### ✅ 完了事項
- **動画生成の同期問題を根本解決**:
    - **実測尺ベースの構築**: これまで「文字数からの推定尺」でHTMLを組んだ後に「実測音声」を生成していたため、ズレが発生していた。これを「音声先行生成 → 実測秒数をHTMLの登場ディレイに反映」というフローに逆転させ、字幕と音声の不一致（被り）を物理的に解消。
- **VoiceVox辞書のネイティブ化**:
    - **姓名分割登録**: `sanitizeForVoiceVox` 内の日本代表選手・監督名を「苗字[全角スペース]名前」形式（例：森保　一）にアップデート。VoiceVoxが自然なイントネーションと「間」で読むように改善。
    - **主要監督の追加**: ポステコグルー、グアルディオラ、アルテタなどの読みも補正。
- **画像取得パイプラインの知能化**:
    - **監督連想ロジック**: DeepSeek V3/Claude に対し「チーム名があれば必ず監督名も抽出せよ」とプロンプトを強化。ニュースが選手個人でも、監督の画像を確実に確保。
    - **ハッシュタグ連動**: 生成されたハッシュタグ（#森保一 など）からも固有名詞を抽出し、Wikipedia検索（wikiWords）に自動反映。

### 🚀 現在の状況
- 全ての改修が VPS にデプロイ完了。
- **負荷テスト中**: VPS上で最新3件の動画同時生成（VoiceVox実測同期モード）を実行し、生成速度と安定性を検証中。

### 📝 次のタスク（ToDo）
1. **生成時間の分析**: 音声実測ステップが追加されたことによる生成時間の変化をチェック。
2. **「監督画像」のヒット率検証**: 森保監督や海外監督の画像が意図通り配置されているか、生成済み動画を目視確認。
3. **自動投稿ロジックの最終調整**: 同期が完璧になったため、いよいよ YouTube への自動アップロードを本格検討。

---

## 📅 本日の進捗 (2026-04-08)

### ✅ 完了事項
- **環境の完全リセット (Revert)**:
    - **今朝の状態へ復帰**: 午後に行った「IDベースの案件管理」「AI(Sonnet)によるサムネイル提案機能」「UIの大幅改修」などを一旦すべて破棄。
    - **理由**: 挙動を安定していた今朝の状態に戻すため。
    - **対象**: `02_reddit_global` ディレクトリ内の全スクリプト、サーバー設定、および関連構成ファイルをコミット `45ac677` (2026-04-07 08:36) の状態に復旧。
- **Git同期**: 修正内容を GitHub に push 済み。VPS側も自動デプロイにより今朝の状態に戻っているはず。

### 🚀 現在の状況
- システム全体が 2026-04-07 朝の安定バージョンで稼働中。
- 今日生成されたデータ類（`data/candidates_2026-04-08.json` など）は保持されているが、管理方式は従来の「インデックスベース」に戻っている。

### 📝 次のタスク（ToDo）
1. **動作確認**: 従来のフローで動画生成・投稿が問題なく行えるか再確認。
2. **再改修の検討**: 午後に行った改修の中で、必要だった要素（Redditブロック回避策など）を、より安全な形で再実装するか検討。

---

## 💡 技術・運用メモ
- 現在の安定版コミット: `45ac677` (をベースにした最新の `2ce917f`)
- IDベース管理 (`MMDD001` 形式) は一旦廃止し、従来の配列インデックスによる管理に戻っている。
- DeepSeek V3 は `AI_PROVIDER=deepseek` で安定稼働中。1記事あたりの選定コストは1円以下。
- VoiceVox 接続エラーは VPS (Docker) 環境であれば解決済み（localhost:50021）。
- 履歴管理 (`seen_history.json`) は 30日間保持を継続。
